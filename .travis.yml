sudo: required
language: generic
os: linux
dist: trusty
services:
  - docker

script: docker run swift /bin/bash -c "git clone https://github.com/matthewfaller/algorithm-practice.git && cd algorithm-practice && git checkout $TRAVIS_COMMIT && swift test" 

# {
#   "os": "linux",
#   "dist": "trusty",
#   "sudo": "required",
#   "group": "stable",
#   "language": "bash",
#   "services": [
#     "docker"
#   ]
# }
# language: swift
# script: swift test

  #.travis.yml